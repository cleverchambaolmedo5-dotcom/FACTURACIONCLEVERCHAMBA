// Shared, hand-written TypeScript types that don't come directly from the
// Prisma Client (which already provides types for every database model).
// Intentionally empty for now — domain types will be added alongside the
// Prisma schema and each module as they're implemented.

export {};
